const backButtonStyles = {
  buttonArea: (horizontal:string, vertical:string) => `
      w-full h-full max-h-[50px] 
      flex ${horizontal} ${vertical}
  `,
  backButtonDescription: `text-[#fff] font-medium text-[1rem]`,
};

export default backButtonStyles;
